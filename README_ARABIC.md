# 🤟 نظام تعلم اللغة الإنجليزية بلغة الإشارة

نظام تفاعلي لتعلم اللغة الإنجليزية باستخدام لغة الإشارة الأمريكية (ASL) مع التعرف الفوري على الإشارات.

## المميزات الرئيسية

### ✨ 1. التعرف على الإشارات في الوقت الفعلي
- استخدام الكاميرا للتعرف الفوري على إشارات اليد
- دعم جميع الأحرف الإنجليزية (A-Z)
- دقة عالية جداً (99.99%)

### 👤 2. شخصية تفاعلية
- شخصية متحركة تعرض الإشارات
- ترجمة تلقائية للكلمات حرفاً بحرف
- واجهة جميلة وسهلة الاستخدام

### 📚 3. نظام التعلم
- إدخال كلمات والتعرف على إشاراتها
- عرض تفاعلي لكل حرف في الكلمة
- تمرين عملي باستخدام الكاميرا

## المتطلبات

### البرامج المطلوبة:
1. **Python 3.8 أو أحدث**
2. **pip** (مدير الحزم في Python)

### المكتبات المطلوبة:
جميع المكتبات موجودة في `requirements.txt`:
- Flask (للباك اند)
- PyTorch (للذكاء الاصطناعي)
- OpenCV (للكاميرا)
- Pillow (لمعالجة الصور)
- NumPy (للعمليات الرياضية)

## التثبيت

### 1. تثبيت المكتبات:
```bash
pip install -r requirements.txt
```

### 2. التأكد من وجود الملفات:
- `pytorch_model.bin` (نموذج التعلم)
- `config.json` (إعدادات النموذج)
- `model.py` (بنية النموذج)

## التشغيل

### تشغيل التطبيق:
```bash
python app.py
```

### الوصول للتطبيق:
افتح المتصفح وانتقل إلى:
```
http://localhost:5000
```

## كيفية الاستخدام

### 1. التعرف على الإشارات:
- اضغط على "تشغيل الكاميرا"
- أظهر إشارة بيدك للكاميرا
- اضغط على "التعرف على الإشارة"
- سيظهر الحرف المكتشف مع نسبة الدقة

### 2. ترجمة الكلمات:
- اكتب كلمة بالإنجليزية في الحقل المخصص
- اضغط على "ترجمة"
- ستظهر الإشارات لكل حرف في الكلمة
- الشخصية ستعرض الإشارات تلقائياً

### 3. التفاعل:
- اضغط على أي حرف لعرض إشارته
- الإشارات تتغير تلقائياً كل ثانيتين
- يمكنك إيقاف التغيير التلقائي بالضغط على حرف معين

## البنية التقنية

### الملفات الرئيسية:
```
sa/
├── app.py                 # تطبيق Flask الرئيسي
├── model.py              # بنية النموذج
├── pytorch_model.bin     # النموذج المدرب
├── config.json           # إعدادات النموذج
├── requirements.txt      # المكتبات المطلوبة
├── templates/
│   └── index.html        # واجهة المستخدم
└── README_ARABIC.md      # هذا الملف
```

### الواجهات البرمجية (API):
- `POST /predict_base64` - التعرف على الإشارة من صورة (base64)
- `POST /translate_word` - ترجمة كلمة إلى أحرف

## متطلبات إضافية للتطوير

إذا كنت تريد تطوير النظام أكثر، قد تحتاج:

### 1. قاعدة بيانات:
- **SQLite** (بسيطة) أو **PostgreSQL** (متقدمة)
- لحفظ تقدم المستخدمين
- لحفظ الكلمات المتعلمة

### 2. نظام المستخدمين:
- تسجيل دخول/تسجيل
- تتبع التقدم
- إحصائيات التعلم

### 3. بيانات تدريب إضافية:
- لديك 86,000 صورة - رائع! 👍
- قد تحتاج لتدريب نموذجك الخاص
- أو تحسين النموذج الحالي

### 4. ميزات إضافية:
- **نظام النقاط والجوائز** (gamification)
- **دروس تفاعلية** (كلمات شائعة، جمل)
- **مقارنة الإشارات** (مدى دقة إشارتك)
- **دعم الفيديو** لعرض إشارات متحركة حقيقية
- **دعم كلمات كاملة** (ليس فقط أحرف)

### 5. تحسينات تقنية:
- **TensorRT** لتسريع الاستدلال (NVIDIA)
- **ONNX Runtime** للسرعة
- **WebSocket** للبث المباشر
- **Redis** للتخزين المؤقت

## الأمان والخصوصية

- جميع معالجة الصور تتم محلياً
- لا يتم إرسال البيانات إلى خوادم خارجية
- يمكن تشغيل النظام بدون اتصال بالإنترنت

## الدعم والمساعدة

إذا واجهت أي مشاكل:
1. تأكد من تثبيت جميع المكتبات
2. تحقق من وجود جميع الملفات
3. تأكد من عمل الكاميرا
4. راجع ملف `requirements.txt`

## الترخيص

MIT License - يمكنك استخدام النظام بحرية

## المساهمات

نرحب بجميع المساهمات لتحسين النظام!

---

**صُنع بـ ❤️ لمساعدة الأطفال في تعلم اللغة الإنجليزية بلغة الإشارة**

