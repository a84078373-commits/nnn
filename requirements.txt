Flask==3.0.0
gunicorn==21.2.0

# Machine Learning
transformers==4.35.0
mediapipe==0.10.7

# Image Processing
opencv-python-headless==4.8.1.78
Pillow==10.1.0
numpy==1.24.3

# ملاحظة: PyTorch يتم تثبيته في Dockerfile بشكل منفصل
# لأننا نحتاج --index-url لتثبيت النسخة CPU فقط
