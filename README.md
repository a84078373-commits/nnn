# نظام تعلم اللغة الإنجليزية بلغة الإشارة (ASL Learning System)

## 📋 المتطلبات

- Python 3.10 أو أعلى
- pip

## 🚀 أوامر التشغيل

### 1. تثبيت المكتبات المطلوبة

```bash
pip install -r requirements.txt
```

### 2. تشغيل التطبيق محلياً

#### على Windows:
```bash
python app.py
```

#### على Linux/Mac:
```bash
chmod +x run.sh
./run.sh
```

أو مباشرة:
```bash
python app.py
```

### 3. الوصول للتطبيق

بعد التشغيل، افتح المتصفح على:
- http://localhost:5000
- http://127.0.0.0.1:5000

## 🌐 النشر على Render

المشروع جاهز للنشر على Render باستخدام ملف `render.yaml`. 

الأمر المحدد في Render:
```bash
gunicorn app:app --bind 0.0.0.0:$PORT
```

## 📝 ملاحظات

- التطبيق يستخدم النموذج من `pytorch_model.bin`
- الإعدادات موجودة في `config.json`
- الصفحة الرئيسية في `templates/index.html`

